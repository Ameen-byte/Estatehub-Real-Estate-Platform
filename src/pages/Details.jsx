import { Link, useParams } from 'react-router-dom'
import { properties as records } from '../data/properties.js'
function Details() { const { id } = useParams(); const record = records.find((item) => item.id === id); if (!record) return <main className="simple-page"><h1>Property not found</h1><Link className="text-link" to="/dashboard">Return to dashboard →</Link></main>; return <main className="simple-page"><Link className="back-link" to="/dashboard">← Back to dashboard</Link><p className="eyebrow">Property detail / {record.id}</p><h1>{record.title}</h1><p className="subtle">{record.location}</p><div className="detail-card"><img src={record.image} alt={record.title} /><div><span className="property-tag">{record.tag}</span><h2>{record.price}</h2><p className="subtle">This dynamic page uses the URL parameter <strong>{id}</strong> to retrieve the selected property.</p><Link className="text-link" to="/dashboard/overview">View all records →</Link></div></div></main> }
export default Details
